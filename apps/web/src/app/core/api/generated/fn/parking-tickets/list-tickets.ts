/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ResponseListParkingTicketsDto } from '../../models/response-list-parking-tickets-dto';

export interface ListTickets$Params {

/**
 * Parking lot ID
 */
  parking: string;
}

export function listTickets(http: HttpClient, rootUrl: string, params: ListTickets$Params, context?: HttpContext): Observable<StrictHttpResponse<ResponseListParkingTicketsDto>> {
  const rb = new RequestBuilder(rootUrl, listTickets.PATH, 'get');
  if (params) {
    rb.query('parking', params.parking, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ResponseListParkingTicketsDto>;
    })
  );
}

listTickets.PATH = '/tickets/list';
