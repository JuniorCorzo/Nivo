/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ResponseListSlotSummaryResponse } from '../../models/response-list-slot-summary-response';

export interface ListSlotSummaries$Params {
  parking: string;
}

export function listSlotSummaries(http: HttpClient, rootUrl: string, params: ListSlotSummaries$Params, context?: HttpContext): Observable<StrictHttpResponse<ResponseListSlotSummaryResponse>> {
  const rb = new RequestBuilder(rootUrl, listSlotSummaries.PATH, 'get');
  if (params) {
    rb.query('parking', params.parking, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ResponseListSlotSummaryResponse>;
    })
  );
}

listSlotSummaries.PATH = '/slots/list/summary';
