/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { CreateSpecialPolicies } from '../../models/create-special-policies';
import { ResponseSpecialPoliciesDto } from '../../models/response-special-policies-dto';

export interface CreateAtSpecialPolicies$Params {
      body: CreateSpecialPolicies
}

export function createAtSpecialPolicies(http: HttpClient, rootUrl: string, params: CreateAtSpecialPolicies$Params, context?: HttpContext): Observable<StrictHttpResponse<ResponseSpecialPoliciesDto>> {
  const rb = new RequestBuilder(rootUrl, createAtSpecialPolicies.PATH, 'post');
  if (params) {
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ResponseSpecialPoliciesDto>;
    })
  );
}

createAtSpecialPolicies.PATH = '/tenants/create/special-policy';
