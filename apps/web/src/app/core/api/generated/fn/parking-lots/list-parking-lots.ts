/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ResponseListParkingLotListItemResponse } from '../../models/response-list-parking-lot-list-item-response';

export interface ListParkingLots$Params {
}

export function listParkingLots(http: HttpClient, rootUrl: string, params?: ListParkingLots$Params, context?: HttpContext): Observable<StrictHttpResponse<ResponseListParkingLotListItemResponse>> {
  const rb = new RequestBuilder(rootUrl, listParkingLots.PATH, 'get');
  if (params) {
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ResponseListParkingLotListItemResponse>;
    })
  );
}

listParkingLots.PATH = '/parking-lots/list';
