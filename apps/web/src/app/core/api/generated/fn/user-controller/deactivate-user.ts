/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { DeactivateUserDto } from '../../models/deactivate-user-dto';
import { ResponseVoid } from '../../models/response-void';

export interface DeactivateUser$Params {
      body: DeactivateUserDto
}

export function deactivateUser(http: HttpClient, rootUrl: string, params: DeactivateUser$Params, context?: HttpContext): Observable<StrictHttpResponse<ResponseVoid>> {
  const rb = new RequestBuilder(rootUrl, deactivateUser.PATH, 'delete');
  if (params) {
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ResponseVoid>;
    })
  );
}

deactivateUser.PATH = '/users/deactivate-user';
