/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ResponseBoolean } from '../../models/response-boolean';

export interface ToggleNotificationPreference$Params {

/**
 * Notification event type
 */
  event: 'RESERVATION_CREATED' | 'TICKET_OPENED' | 'TICKET_CLOSED' | 'PAYMENT_COMPLETED' | 'PAYMENT_CHECKOUT' | 'USER_SELF_REGISTERED' | 'USER_INVITED' | 'USER_INVITATION_ACCEPTED' | 'USER_ROLE_ASSIGNED';

/**
 * Notification delivery channel
 */
  channel: 'EMAIL' | 'WHATSAPP';
}

export function toggleNotificationPreference(http: HttpClient, rootUrl: string, params: ToggleNotificationPreference$Params, context?: HttpContext): Observable<StrictHttpResponse<ResponseBoolean>> {
  const rb = new RequestBuilder(rootUrl, toggleNotificationPreference.PATH, 'patch');
  if (params) {
    rb.query('event', params.event, {});
    rb.query('channel', params.channel, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ResponseBoolean>;
    })
  );
}

toggleNotificationPreference.PATH = '/notification-preferences/toggle';
