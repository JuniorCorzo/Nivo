/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { EmailCheckOutCommand } from '../../models/email-check-out-command';
import { NoSendCheckOutCommand } from '../../models/no-send-check-out-command';
import { ResponsePaymentsDto } from '../../models/response-payments-dto';
import { SmsCheckOutCommand } from '../../models/sms-check-out-command';

export interface CheckOutVehicle$Params {
      body: (EmailCheckOutCommand | NoSendCheckOutCommand | SmsCheckOutCommand)
}

export function checkOutVehicle(http: HttpClient, rootUrl: string, params: CheckOutVehicle$Params, context?: HttpContext): Observable<StrictHttpResponse<ResponsePaymentsDto>> {
  const rb = new RequestBuilder(rootUrl, checkOutVehicle.PATH, 'post');
  if (params) {
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ResponsePaymentsDto>;
    })
  );
}

checkOutVehicle.PATH = '/tickets/check-out';
