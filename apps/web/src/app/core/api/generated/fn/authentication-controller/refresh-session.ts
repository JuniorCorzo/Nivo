/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ResponseAuthenticationResponseDto } from '../../models/response-authentication-response-dto';

export interface RefreshSession$Params {
}

export function refreshSession(http: HttpClient, rootUrl: string, params?: RefreshSession$Params, context?: HttpContext): Observable<StrictHttpResponse<ResponseAuthenticationResponseDto>> {
  const rb = new RequestBuilder(rootUrl, refreshSession.PATH, 'post');
  if (params) {
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ResponseAuthenticationResponseDto>;
    })
  );
}

refreshSession.PATH = '/auth/refresh';
