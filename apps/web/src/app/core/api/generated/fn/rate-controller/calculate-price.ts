/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ResponsePriceDetailed } from '../../models/response-price-detailed';

export interface CalculatePrice$Params {
  ticketId: string;
}

export function calculatePrice(http: HttpClient, rootUrl: string, params: CalculatePrice$Params, context?: HttpContext): Observable<StrictHttpResponse<ResponsePriceDetailed>> {
  const rb = new RequestBuilder(rootUrl, calculatePrice.PATH, 'get');
  if (params) {
    rb.path('ticketId', params.ticketId, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ResponsePriceDetailed>;
    })
  );
}

calculatePrice.PATH = '/rates/{ticketId}/calculate';
