/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ResponseVoid } from '../../models/response-void';

export interface DeleteSlotGroup$Params {

/**
 * Parking lot ID
 */
  parkingId: string;

/**
 * Slot type (e.g. STANDARD, COMPACT)
 */
  slotType: 'CAR' | 'MOTORCYCLE' | 'BIKE' | 'ELECTRIC_VEHICLE' | 'DISABLED';

/**
 * Prefix filter
 */
  prefix?: string;

/**
 * Zone filter
 */
  zone?: string;
}

export function deleteSlotGroup(http: HttpClient, rootUrl: string, params: DeleteSlotGroup$Params, context?: HttpContext): Observable<StrictHttpResponse<ResponseVoid>> {
  const rb = new RequestBuilder(rootUrl, deleteSlotGroup.PATH, 'delete');
  if (params) {
    rb.path('parkingId', params.parkingId, {});
    rb.query('slotType', params.slotType, {});
    rb.query('prefix', params.prefix, {});
    rb.query('zone', params.zone, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ResponseVoid>;
    })
  );
}

deleteSlotGroup.PATH = '/parking-lots/{parkingId}/slots/groups';
