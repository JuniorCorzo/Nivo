/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ResponseParkingLotsResponse } from '../../models/response-parking-lots-response';
import { UpsertParkingLotsRequest } from '../../models/upsert-parking-lots-request';

export interface CreateParkingLots$Params {
      body: UpsertParkingLotsRequest
}

export function createParkingLots(http: HttpClient, rootUrl: string, params: CreateParkingLots$Params, context?: HttpContext): Observable<StrictHttpResponse<ResponseParkingLotsResponse>> {
  const rb = new RequestBuilder(rootUrl, createParkingLots.PATH, 'post');
  if (params) {
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ResponseParkingLotsResponse>;
    })
  );
}

createParkingLots.PATH = '/parking-lots/create';
