/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ResponseRatesDto } from '../../models/response-rates-dto';
import { UpdateRate } from '../../models/update-rate';

export interface UpdateRate$Params {
      body: UpdateRate
}

export function updateRate(http: HttpClient, rootUrl: string, params: UpdateRate$Params, context?: HttpContext): Observable<StrictHttpResponse<ResponseRatesDto>> {
  const rb = new RequestBuilder(rootUrl, updateRate.PATH, 'put');
  if (params) {
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ResponseRatesDto>;
    })
  );
}

updateRate.PATH = '/rates/update';
