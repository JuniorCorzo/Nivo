/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ResponseVoid } from '../../models/response-void';

export interface DeleteSlot$Params {

/**
 * Slot ID
 */
  slotId: string;
}

export function deleteSlot(http: HttpClient, rootUrl: string, params: DeleteSlot$Params, context?: HttpContext): Observable<StrictHttpResponse<ResponseVoid>> {
  const rb = new RequestBuilder(rootUrl, deleteSlot.PATH, 'delete');
  if (params) {
    rb.path('slotId', params.slotId, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ResponseVoid>;
    })
  );
}

deleteSlot.PATH = '/slots/delete/{slotId}';
