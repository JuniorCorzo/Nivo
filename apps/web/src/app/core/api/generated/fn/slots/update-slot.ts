/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ResponseSlotResponse } from '../../models/response-slot-response';
import { UpdateSlotRequest } from '../../models/update-slot-request';

export interface UpdateSlot$Params {
      body: UpdateSlotRequest
}

export function updateSlot(http: HttpClient, rootUrl: string, params: UpdateSlot$Params, context?: HttpContext): Observable<StrictHttpResponse<ResponseSlotResponse>> {
  const rb = new RequestBuilder(rootUrl, updateSlot.PATH, 'put');
  if (params) {
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ResponseSlotResponse>;
    })
  );
}

updateSlot.PATH = '/slots/update';
