/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { RegisterTenantDto } from '../../models/register-tenant-dto';
import { ResponseUserDto } from '../../models/response-user-dto';

export interface RegisterTenant$Params {
      body: RegisterTenantDto
}

export function registerTenant(http: HttpClient, rootUrl: string, params: RegisterTenant$Params, context?: HttpContext): Observable<StrictHttpResponse<ResponseUserDto>> {
  const rb = new RequestBuilder(rootUrl, registerTenant.PATH, 'post');
  if (params) {
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ResponseUserDto>;
    })
  );
}

registerTenant.PATH = '/tenants/register';
