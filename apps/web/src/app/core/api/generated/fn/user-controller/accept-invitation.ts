/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { CreatedUserDto } from '../../models/created-user-dto';
import { ResponseUserInvitationsDto } from '../../models/response-user-invitations-dto';

export interface AcceptInvitation$Params {
  token: string;
      body: CreatedUserDto
}

export function acceptInvitation(http: HttpClient, rootUrl: string, params: AcceptInvitation$Params, context?: HttpContext): Observable<StrictHttpResponse<ResponseUserInvitationsDto>> {
  const rb = new RequestBuilder(rootUrl, acceptInvitation.PATH, 'patch');
  if (params) {
    rb.path('token', params.token, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ResponseUserInvitationsDto>;
    })
  );
}

acceptInvitation.PATH = '/users/accept-invitation/{token}';
