/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ResponseIterableRatesDto } from '../../models/response-iterable-rates-dto';

export interface ShowRatesByParkingId$Params {

/**
 * Parking lot ID
 */
  parkingId: string;
}

export function showRatesByParkingId(http: HttpClient, rootUrl: string, params: ShowRatesByParkingId$Params, context?: HttpContext): Observable<StrictHttpResponse<ResponseIterableRatesDto>> {
  const rb = new RequestBuilder(rootUrl, showRatesByParkingId.PATH, 'get');
  if (params) {
    rb.path('parkingId', params.parkingId, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ResponseIterableRatesDto>;
    })
  );
}

showRatesByParkingId.PATH = '/parking-lots/{parkingId}/rates';
