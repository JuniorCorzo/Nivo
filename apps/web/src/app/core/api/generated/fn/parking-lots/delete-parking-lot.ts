/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ResponseVoid } from '../../models/response-void';

export interface DeleteParkingLot$Params {

/**
 * Parking lot ID
 */
  parkingId: string;
}

export function deleteParkingLot(http: HttpClient, rootUrl: string, params: DeleteParkingLot$Params, context?: HttpContext): Observable<StrictHttpResponse<ResponseVoid>> {
  const rb = new RequestBuilder(rootUrl, deleteParkingLot.PATH, 'delete');
  if (params) {
    rb.path('parkingId', params.parkingId, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ResponseVoid>;
    })
  );
}

deleteParkingLot.PATH = '/parking-lots/{parkingId}';
