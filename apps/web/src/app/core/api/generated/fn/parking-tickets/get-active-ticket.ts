/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ResponseParkingTicketsDto } from '../../models/response-parking-tickets-dto';

export interface GetActiveTicket$Params {

/**
 * Slot ID
 */
  slot: string;
}

export function getActiveTicket(http: HttpClient, rootUrl: string, params: GetActiveTicket$Params, context?: HttpContext): Observable<StrictHttpResponse<ResponseParkingTicketsDto>> {
  const rb = new RequestBuilder(rootUrl, getActiveTicket.PATH, 'get');
  if (params) {
    rb.query('slot', params.slot, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ResponseParkingTicketsDto>;
    })
  );
}

getActiveTicket.PATH = '/tickets/active';
