/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { BatchCreateSlotRequest } from '../../models/batch-create-slot-request';
import { ResponseVoid } from '../../models/response-void';

export interface CreateSlots$Params {
      body: BatchCreateSlotRequest
}

export function createSlots(http: HttpClient, rootUrl: string, params: CreateSlots$Params, context?: HttpContext): Observable<StrictHttpResponse<ResponseVoid>> {
  const rb = new RequestBuilder(rootUrl, createSlots.PATH, 'post');
  if (params) {
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ResponseVoid>;
    })
  );
}

createSlots.PATH = '/slots/create';
