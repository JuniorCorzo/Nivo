/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ModifyRolDto } from '../../models/modify-rol-dto';
import { ResponseUserDto } from '../../models/response-user-dto';

export interface ModifyRol$Params {
      body: ModifyRolDto
}

export function modifyRol(http: HttpClient, rootUrl: string, params: ModifyRol$Params, context?: HttpContext): Observable<StrictHttpResponse<ResponseUserDto>> {
  const rb = new RequestBuilder(rootUrl, modifyRol.PATH, 'patch');
  if (params) {
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ResponseUserDto>;
    })
  );
}

modifyRol.PATH = '/users/modify-rol';
