/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext } from '@angular/common/http';
import { Injectable } from '@angular/core';
import { Observable } from 'rxjs';
import { map } from 'rxjs/operators';

import { BaseService } from '../base-service';
import { ApiConfiguration } from '../api-configuration';
import { StrictHttpResponse } from '../strict-http-response';

import { calculatePrice } from '../fn/rates/calculate-price';
import { CalculatePrice$Params } from '../fn/rates/calculate-price';
import { deleteRate } from '../fn/rates/delete-rate';
import { DeleteRate$Params } from '../fn/rates/delete-rate';
import { ResponsePriceDetailed } from '../models/response-price-detailed';
import { ResponseRatesDto } from '../models/response-rates-dto';
import { ResponseVoid } from '../models/response-void';
import { updateRate } from '../fn/rates/update-rate';
import { UpdateRate$Params } from '../fn/rates/update-rate';


/**
 * Parking rate calculation, updating and deletion
 */
@Injectable({ providedIn: 'root' })
export class RatesService extends BaseService {
  constructor(config: ApiConfiguration, http: HttpClient) {
    super(config, http);
  }

  /** Path part for operation `updateRate()` */
  static readonly UpdateRatePath = '/rates/update';

  /**
   * Update tariff rate.
   *
   * Updates the configuration of an existing tariff rate
   *
   * This method provides access to the full `HttpResponse`, allowing access to response headers.
   * To access only the response body, use `updateRate()` instead.
   *
   * This method sends `application/json` and handles request body of type `application/json`.
   */
  updateRate$Response(params: UpdateRate$Params, context?: HttpContext): Observable<StrictHttpResponse<ResponseRatesDto>> {
    const obs = updateRate(this.http, this.rootUrl, params, context);
    return obs;
  }

  /**
   * Update tariff rate.
   *
   * Updates the configuration of an existing tariff rate
   *
   * This method provides access only to the response body.
   * To access the full response (for headers, for example), `updateRate$Response()` instead.
   *
   * This method sends `application/json` and handles request body of type `application/json`.
   */
  updateRate(params: UpdateRate$Params, context?: HttpContext): Observable<ResponseRatesDto> {
    const resp = this.updateRate$Response(params, context);
    return resp.pipe(
      map((r: StrictHttpResponse<ResponseRatesDto>): ResponseRatesDto => r.body)
    );
  }

  /** Path part for operation `calculatePrice()` */
  static readonly CalculatePricePath = '/rates/{ticketId}/calculate';

  /**
   * Calculate parking ticket price.
   *
   * Calculates the detailed amount to charge for a parking ticket based on duration and rates
   *
   * This method provides access to the full `HttpResponse`, allowing access to response headers.
   * To access only the response body, use `calculatePrice()` instead.
   *
   * This method doesn't expect any request body.
   */
  calculatePrice$Response(params: CalculatePrice$Params, context?: HttpContext): Observable<StrictHttpResponse<ResponsePriceDetailed>> {
    const obs = calculatePrice(this.http, this.rootUrl, params, context);
    return obs;
  }

  /**
   * Calculate parking ticket price.
   *
   * Calculates the detailed amount to charge for a parking ticket based on duration and rates
   *
   * This method provides access only to the response body.
   * To access the full response (for headers, for example), `calculatePrice$Response()` instead.
   *
   * This method doesn't expect any request body.
   */
  calculatePrice(params: CalculatePrice$Params, context?: HttpContext): Observable<ResponsePriceDetailed> {
    const resp = this.calculatePrice$Response(params, context);
    return resp.pipe(
      map((r: StrictHttpResponse<ResponsePriceDetailed>): ResponsePriceDetailed => r.body)
    );
  }

  /** Path part for operation `deleteRate()` */
  static readonly DeleteRatePath = '/rates/{id}/delete';

  /**
   * Delete tariff rate.
   *
   * Deletes a tariff rate by its ID
   *
   * This method provides access to the full `HttpResponse`, allowing access to response headers.
   * To access only the response body, use `deleteRate()` instead.
   *
   * This method doesn't expect any request body.
   */
  deleteRate$Response(params: DeleteRate$Params, context?: HttpContext): Observable<StrictHttpResponse<ResponseVoid>> {
    const obs = deleteRate(this.http, this.rootUrl, params, context);
    return obs;
  }

  /**
   * Delete tariff rate.
   *
   * Deletes a tariff rate by its ID
   *
   * This method provides access only to the response body.
   * To access the full response (for headers, for example), `deleteRate$Response()` instead.
   *
   * This method doesn't expect any request body.
   */
  deleteRate(params: DeleteRate$Params, context?: HttpContext): Observable<ResponseVoid> {
    const resp = this.deleteRate$Response(params, context);
    return resp.pipe(
      map((r: StrictHttpResponse<ResponseVoid>): ResponseVoid => r.body)
    );
  }

}
