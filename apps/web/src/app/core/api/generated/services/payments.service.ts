/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext } from '@angular/common/http';
import { Injectable } from '@angular/core';
import { Observable } from 'rxjs';
import { map } from 'rxjs/operators';

import { BaseService } from '../base-service';
import { ApiConfiguration } from '../api-configuration';
import { StrictHttpResponse } from '../strict-http-response';

import { confirm } from '../fn/payments/confirm';
import { Confirm$Params } from '../fn/payments/confirm';


/**
 * Payment webhook confirmation and processing
 */
@Injectable({ providedIn: 'root' })
export class PaymentsService extends BaseService {
  constructor(config: ApiConfiguration, http: HttpClient) {
    super(config, http);
  }

  /** Path part for operation `confirm()` */
  static readonly ConfirmPath = '/payments/confirmation';

  /**
   * Payment confirmation webhook.
   *
   * Receives payment confirmation webhook callbacks from the payment gateway
   *
   * This method provides access to the full `HttpResponse`, allowing access to response headers.
   * To access only the response body, use `confirm()` instead.
   *
   * This method sends `application/x-www-form-urlencoded` and handles request body of type `application/x-www-form-urlencoded`.
   */
  confirm$Response(params?: Confirm$Params, context?: HttpContext): Observable<StrictHttpResponse<{
}>> {
    const obs = confirm(this.http, this.rootUrl, params, context);
    return obs;
  }

  /**
   * Payment confirmation webhook.
   *
   * Receives payment confirmation webhook callbacks from the payment gateway
   *
   * This method provides access only to the response body.
   * To access the full response (for headers, for example), `confirm$Response()` instead.
   *
   * This method sends `application/x-www-form-urlencoded` and handles request body of type `application/x-www-form-urlencoded`.
   */
  confirm(params?: Confirm$Params, context?: HttpContext): Observable<{
}> {
    const resp = this.confirm$Response(params, context);
    return resp.pipe(
      map((r: StrictHttpResponse<{
}>): {
} => r.body)
    );
  }

}
