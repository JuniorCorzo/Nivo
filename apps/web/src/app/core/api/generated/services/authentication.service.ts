/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext } from '@angular/common/http';
import { Injectable } from '@angular/core';
import { Observable } from 'rxjs';
import { map } from 'rxjs/operators';

import { BaseService } from '../base-service';
import { ApiConfiguration } from '../api-configuration';
import { StrictHttpResponse } from '../strict-http-response';

import { login } from '../fn/authentication/login';
import { Login$Params } from '../fn/authentication/login';
import { logout } from '../fn/authentication/logout';
import { Logout$Params } from '../fn/authentication/logout';
import { refreshSession } from '../fn/authentication/refresh-session';
import { RefreshSession$Params } from '../fn/authentication/refresh-session';
import { ResponseAuthenticationResponseDto } from '../models/response-authentication-response-dto';


/**
 * Authentication and session management endpoints
 */
@Injectable({ providedIn: 'root' })
export class AuthenticationService extends BaseService {
  constructor(config: ApiConfiguration, http: HttpClient) {
    super(config, http);
  }

  /** Path part for operation `refreshSession()` */
  static readonly RefreshSessionPath = '/auth/refresh';

  /**
   * Refresh session tokens.
   *
   * Generates new JWT access and refresh tokens using the refreshToken cookie
   *
   * This method provides access to the full `HttpResponse`, allowing access to response headers.
   * To access only the response body, use `refreshSession()` instead.
   *
   * This method doesn't expect any request body.
   */
  refreshSession$Response(params?: RefreshSession$Params, context?: HttpContext): Observable<StrictHttpResponse<ResponseAuthenticationResponseDto>> {
    const obs = refreshSession(this.http, this.rootUrl, params, context);
    return obs;
  }

  /**
   * Refresh session tokens.
   *
   * Generates new JWT access and refresh tokens using the refreshToken cookie
   *
   * This method provides access only to the response body.
   * To access the full response (for headers, for example), `refreshSession$Response()` instead.
   *
   * This method doesn't expect any request body.
   */
  refreshSession(params?: RefreshSession$Params, context?: HttpContext): Observable<ResponseAuthenticationResponseDto> {
    const resp = this.refreshSession$Response(params, context);
    return resp.pipe(
      map((r: StrictHttpResponse<ResponseAuthenticationResponseDto>): ResponseAuthenticationResponseDto => r.body)
    );
  }

  /** Path part for operation `logout()` */
  static readonly LogoutPath = '/auth/logout';

  /**
   * User logout.
   *
   * Clears the authentication session and refresh token cookie
   *
   * This method provides access to the full `HttpResponse`, allowing access to response headers.
   * To access only the response body, use `logout()` instead.
   *
   * This method doesn't expect any request body.
   */
  logout$Response(params?: Logout$Params, context?: HttpContext): Observable<StrictHttpResponse<void>> {
    const obs = logout(this.http, this.rootUrl, params, context);
    return obs;
  }

  /**
   * User logout.
   *
   * Clears the authentication session and refresh token cookie
   *
   * This method provides access only to the response body.
   * To access the full response (for headers, for example), `logout$Response()` instead.
   *
   * This method doesn't expect any request body.
   */
  logout(params?: Logout$Params, context?: HttpContext): Observable<void> {
    const resp = this.logout$Response(params, context);
    return resp.pipe(
      map((r: StrictHttpResponse<void>): void => r.body)
    );
  }

  /** Path part for operation `login()` */
  static readonly LoginPath = '/auth/login';

  /**
   * User login.
   *
   * Authenticates user credentials and returns JWT access token with refresh token cookie
   *
   * This method provides access to the full `HttpResponse`, allowing access to response headers.
   * To access only the response body, use `login()` instead.
   *
   * This method sends `application/json` and handles request body of type `application/json`.
   */
  login$Response(params: Login$Params, context?: HttpContext): Observable<StrictHttpResponse<ResponseAuthenticationResponseDto>> {
    const obs = login(this.http, this.rootUrl, params, context);
    return obs;
  }

  /**
   * User login.
   *
   * Authenticates user credentials and returns JWT access token with refresh token cookie
   *
   * This method provides access only to the response body.
   * To access the full response (for headers, for example), `login$Response()` instead.
   *
   * This method sends `application/json` and handles request body of type `application/json`.
   */
  login(params: Login$Params, context?: HttpContext): Observable<ResponseAuthenticationResponseDto> {
    const resp = this.login$Response(params, context);
    return resp.pipe(
      map((r: StrictHttpResponse<ResponseAuthenticationResponseDto>): ResponseAuthenticationResponseDto => r.body)
    );
  }

}
